# MIRROR PRODUCTION ENVIRONMENT
# Copy this file to .env on the mirror server
# This configuration is for internal production (hostname == "mirror")

# Environment indicator (auto-detected by hostname)
# The system will auto-detect MIRROR environment if hostname contains "mirror"
ENVIRONMENT=mirror

# Database Configuration (PostgreSQL in Docker)
# These settings connect the app container to the database container
DB_USER=bdoor_user
DB_PASSWORD=bdoor_password
DB_HOST=postgres  # Docker service name in docker-compose
DB_PORT=5432
DB_NAME=bdoor_postgres

# Mirror-specific database connection (optional, for explicit config)
MIRROR_DB_HOST=postgres
MIRROR_DB_PORT=5432
MIRROR_DB_USER=bdoor_user
MIRROR_DB_PASSWORD=bdoor_password
MIRROR_DB_NAME=bdoor_postgres

# Application Settings
PORT=8080
DEBUG=False
SECRET_KEY=your-mirror-secret-key-change-this-to-strong-random-value

# Features (automatically configured for MIRROR)
# - No authentication required (trusted internal network)
# - Database sync enabled (syncs with local dev)
# - Full features enabled
# - Stable build only
# - Production monitoring enabled
# - No experimental features

# Logging
LOG_LEVEL=INFO

