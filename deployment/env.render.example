# RENDER CLOUD PRODUCTION ENVIRONMENT
# Configure these as Environment Variables in Render Dashboard
# This configuration is for cloud deployment (accessible from internet)

# Environment indicator (auto-detected by RENDER env var)
# Render automatically sets this, no need to configure
# RENDER=true  # Automatically set by Render

# Neon Database Configuration (Cloud PostgreSQL)
# Get these from your Neon dashboard
NEON_HOST=your-neon-host.neon.tech
NEON_PORT=5432
NEON_USER=your-neon-username
NEON_PASSWORD=your-neon-password
NEON_DATABASE_NAME=your-database-name

# Use Neon flag (auto-detected, but can be explicit)
USE_NEON=true

# Application Settings
# PORT is automatically set by Render to 10000
# PORT=10000  # Automatically set by Render
DEBUG=False
SECRET_KEY=your-render-secret-key-use-strong-random-value-here

# Authentication Settings (REQUIRED for Render)
# All pages require authentication on Render
AUTH_USERNAME=your-username
AUTH_PASSWORD=your-secure-password

# Features (automatically configured for RENDER)
# - Authentication REQUIRED on all pages
# - SSL enabled
# - CORS restricted
# - Production monitoring enabled
# - No database sync (cloud database is source of truth)
# - No experimental features

# Logging
LOG_LEVEL=INFO

# Optional: External API Keys
# Add any external API keys needed for your application
# WEATHER_API_KEY=your-weather-api-key
# STOCK_API_KEY=your-stock-api-key

