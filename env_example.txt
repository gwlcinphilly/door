# Database Configuration
# Set to 'true' to use Neon database, 'false' for local PostgreSQL
USE_NEON=false

# Local PostgreSQL Configuration (when USE_NEON=false)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bdoor_postgres
DB_USER=bdoor_user
DB_PASSWORD=bdoor_password

# Neon Database Configuration (when USE_NEON=true)
NEON_HOST=ep-icy-violet-adv1dwix.c-2.us-east-1.aws.neon.tech
NEON_PORT=5432
NEON_DATABASE_NAME=neondb
NEON_USER=neondb_owner
NEON_PASSWORD=npg_EntrThk1V8KI

# Render Deployment Configuration
# These are automatically set by Render, but you can override if needed
# PORT=10000
# RENDER=true
